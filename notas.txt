$ git log(historial de cambios)
$ git checkout NOMBRE DEL ARCHIVO( hace un regreso de los archivos a como estan en el ultimos commit en el repositorio)
$ git reset( me indica que ficheros has sido modificados pero que aun no se han echo commit )
$ git log --graph
$ git log --graph --pretty=oneline
$ git log --graph --decorate --all --oneline

$ git diff (nos muestra en sonsola todos los cambios que se han generado en cad una de las lineas de los ficheros )

$ git reset --hard 9dcabd3 (se hace un regreso al id de commit indicado)

$ git reflog (es un historial completo de todos los cambios en y commits)


$ git merge -nombre de la rama -(escribimos el nombre de la rama con el que queremos unir los cambios )

$ git tag(etiquetas que se hacen en commits en momentos importantes)

$ git stash  (guardamos los cambios en una especie de nube pero sin hacer momit momentaneamente se eliminan esos cambios hasta el untimop commit para despues recuperarlos  y seguir trabajando en el codigo:)

$ git stash list( nos muestra la lista de stash que hemos realizado)

$ git stash drop (elimina todo loo que tengamos en la lista de stash)

$git fetch ( descarga el historial de cambios en git hub pero no descarga los cambios)


$ git merge (descarga los cambios sin el historial de donde indiques )

$ git pull(descarga el historial y los cambios de hithub)


